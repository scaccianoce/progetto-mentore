SECRETS / VAULT - NOMI DA RICREARE, NON VALORI
================================================

Dall'inventario corrente risultano riferimenti ai seguenti nomi:

Vault PostgreSQL usato dal Cron:
- notifiche_project_url
- notifiche_publishable_key
- notifiche_cron_secret

Vault PostgreSQL usato dalla sincronizzazione mentoraggi Google Sheet:
- mentoraggi_google_sheet_project_url
- mentoraggi_google_sheet_secret_key

Edge Function notifiche-invia (dal sorgente condiviso in chat):
- FIREBASE_SERVICE_ACCOUNT_JSON
- NOTIFICHE_CRON_SECRET
- PROJECT_SECRET_KEY oppure SUPABASE_SECRET_KEYS

Edge Function mentoraggi-google-sheet:
- GOOGLE_SCRIPT_URL (fallback, facoltativo dopo la configurazione dall'app)
- GOOGLE_SCRIPT_SECRET (fallback, facoltativo dopo la configurazione dall'app)
- google_sheet_sync (secret richiesto dalla configurazione withSupabase)

Configurazioni Google Sheet create dall'app:
- ogni GOOGLE_SCRIPT_SECRET viene salvato cifrato in Supabase Vault;
- il valore non viene restituito all'app e non deve essere versionato.

Variabili Supabase standard fornite automaticamente alle Edge Functions:
- SUPABASE_URL
- chiavi Supabase standard dell'ambiente

IMPORTANTE:
- non versionare mai i valori reali;
- nel nuovo progetto usare URL e chiavi del NUOVO ambiente;
- il presente file è solo un inventario dei nomi.
